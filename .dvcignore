# Add patterns of files dvc should ignore, which could improve
# the performance. Learn more at
# https://dvc.org/doc/user-guide/dvcignore

# Snapshots are intentionally not tracked by DVC (they are a local audit
# trail, not a reproducible artifact of the pipeline).
snapshots/